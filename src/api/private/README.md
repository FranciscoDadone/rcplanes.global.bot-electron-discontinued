# Credentials file format
 - name: 'credentials.js'

 - Code:
 
        const access_token  = ''
        const client_secret = ''
        const client_id     = ''
        const page_id       = ''
        const ig_account_id = ''

        module.exports = { access_token, client_secret, client_id, page_id, ig_account_id }
